# Sebastian Musiał — Certificates

Premium dark/minimal portfolio landing page.

## Start
Open `index.html`.

## Deploy
Upload the whole folder to Vercel, Netlify or GitHub Pages.

## Add certificatess
Add a PDF to `certificatess/`, duplicate an `<article class="cert">` block in `index.html`, and update its metadata and links.
